-- edit schema to add columns that do not exist in 2015/2016 data to table to fit 2020 data
ALTER TABLE taxi_info
ADD COLUMN IF NOT EXISTS congestion_surcharge INT;

ALTER TABLE taxi_info
ADD COLUMN IF NOT EXISTS PULocationID INT;

ALTER TABLE taxi_info
ADD COLUMN IF NOT EXISTS DOLocationID INT;

-- confirm columns exist
SELECT * FROM taxi_info LIMIT 5;